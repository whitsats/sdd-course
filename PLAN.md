# 详细学习计划 · 17 节

> 每节 1–2 小时。每节结构固定：**前置 → 目标 → 动手步骤 → 交付物 → 验收标准 → 踩坑预警 → 术语**。
> 教程正文写在 `lessons/` 下；本文件是总纲与课时清单。

---

## 阶段概览

| 阶段 | 节 | 主题 | 案例 | 交付物 |
|------|-----|------|------|--------|
| **一 · 认知与地基** | L00–L02 | 工具链、心智模型、宪法 | 无（hello-sdd 空跑） | 可用环境 + 宪法 |
| **二 · 最短闭环** | L03–L05 | Specify → Plan/Tasks → Implement | A. focuslog | 可运行 CLI |
| **三 · 规格锚定** | L06–L09 | 全流程 + CI 门禁 | B. taskflow | CI 强制的 spec-anchored 项目 ⭐ |
| **四 · 进阶自主化** | L10–L12 | 规格即源、生态、团队化 | C. rulesmith + 存量仓库 | 生成式项目 + 落地清单 |
| **五 · 可选进阶** | L13 | 约束能放在**多早** | D. billflow（Java） | 编译期就被拦住的工程 |
| **六 · 可选进阶** | L14 | 约束能放在**多晚** | E. stockflow（SQLite） | 绕过应用也拦得住的 schema |
| **七 · 可选进阶** | L15 | 约束作用在**执行者身上** | A. focuslog（复用其规格） | 评测集 + 一轮判定与翻转记录 |
| **八 · 可选进阶** | L16 | 上线之后**谁站岗** | E. stockflow（复用巡检）+ B（复用错误码投影） | 探针清单 + 契约交叉验证 + flag 对账 |

**三个严谨度等级的关系**（先记住这张图，L01 会展开）：

```
Spec-First          规格为最初的种子，之后允许代码漂移
   │  加「活的测试 + CI 强制」
   ▼
Spec-Anchored 🎯    规格与代码共同演化，测试强制二者对齐   ← 生产系统的甜点区
   │  信任生成能力，停止手写代码
   ▼
Spec-as-Source      人只改规格，代码 100% 生成，永不手改   ← 目前仍是探索区
```

---

# 阶段一 · 认知与地基

## L00 · 环境准备与首次空跑

| 项 | 内容 |
|----|------|
| **时长** | 60 min |
| **前置** | 无 |
| **目标** | 装好 `specify` CLI；理解 Spec Kit 产出的目录结构；完整空跑一次流水线，建立「斜杠命令 = 流程步骤」的手感 |
| **动手** | ① `uv tool install specify-cli` ② `specify init hello-sdd --integration <你的 agent>` ③ 观察 `.specify/` 与 `.specify/templates/` ④ 依次调用 `constitution → specify → plan → tasks`，**只看产出不写代码** |
| **交付物** | `hello-sdd/` 目录 + 你能画出 Spec Kit 的工作流图 |
| **验收** | 你能不看文档说出：`.specify/feature.json` 是干什么的；下一阶段在哪个文件里能找到要被实现的任务 |
| **踩坑** | ① Windows 下脚本风格选错（建议 `--script ps`）② 以为斜杠命令是终端命令——它们是 **agent 聊天里的 skill**，只有安装和 init 用终端 ③ 无 TTY 环境下 init 会卡在箭头选择器，要加 `--non-interactive` |
| **术语** | `specify init`、integration、skill、`.specify/feature.json` |

> ⚠️ 本节的特殊要求：**空跑阶段故意不写代码**。先建立对产物流的直觉，再谈实现。这是 SDD 的入门仪式。

---

## L01 · 为什么是 SDD：心智模型与成熟度模型

| 项 | 内容 |
|----|------|
| **时长** | 60 min |
| **前置** | L00 |
| **目标** | 建立三个核心判断力：① 什么时候规格值得写 ② 传统设计文档与 SDD 规格的本质差异 ③ 自己该停在哪一级严谨度 |
| **关键对比** | 传统设计文档 vs SDD 规格：**读者**（人 / 人与 agent）、**约束力**（建议性 / 可执行，漂移即红灯）、**生命周期**（腐化成考古现场 / 活的、持续校验）、**存放位置**（wiki / 仓库 + CI） |
| **反面教材** | vibe coding 的三种典型破产方式：需求漂移、上下文丢失、无法验证「做完了」。用你自己踩过的坑对照 |
| **动手** | 写一页心智模型笔记：我的哪个项目适合 spec-first？哪个必须 spec-anchored？ |
| **交付物** | 一页笔记（建议 `notes/L01-心智模型.md`） |
| **验收** | 能回答：如果一个需求三天不变、只写一次就扔，值不值得写完整规格？为什么？ |
| **踩坑** | 把 SDD 当宗教——**过度规格化也是一种浪费**。L01 会给出「不值得写规格」的四类场景 |
| **术语** | vibe coding、spec-first / spec-anchored / spec-as-source、drift（漂移） |

---

## L02 · Constitution：给项目立宪

| 项 | 内容 |
|----|------|
| **时长** | 90 min |
| **前置** | L01 |
| **目标** | 写出第一份真正的工程约束文件。理解宪法的作用不是「描述需求」，而是**约束后续每一个决策的评判标准** |
| **动手** | ① 调用 `/speckit-constitution` 并传入你的原则 ② 逐条审查 ③ 故意写一版「太长的宪法」，观察 agent 如何开始忽略它，然后精简 |
| **宪法七类条款** | 技术栈与版本 / 架构边界 / 测试与质量门禁 / 安全与隐私 / 性能预算 / 可观测性 / 文档与提交规范 |
| **交付物** | `memory/constitution.md`（或 Spec Kit 当前实际路径），控制在 **1 页以内** |
| **验收** | ① 每条原则都能被某次 code review 用作「不给过」的理由 ② 没有任何一条是「我们要写高质量代码」式的空话 |
| **踩坑** | ① 宪法写 8 页 → agent 上下文里变成噪声 ② 写进具体功能需求（那是 spec 的活）③ 和工具默认模板打架 |
| **术语** | constitution、guardrail、quality gate |

---

# 阶段二 · 走通最短闭环

> 案例：**项目 A `focuslog`**（Python CLI，专注日志）。选它是因为纯逻辑、无 UI、无网络，**规格与实现的因果链最短**，你能清楚看到「一句话怎么变成一行代码」。
> 本阶段使用**短路径**：`specify → plan → tasks → implement`，先不引入质量门禁。

## L03 · Specify：把意图写成规格

| 项 | 内容 |
|----|------|
| **时长** | 120 min |
| **前置** | L00–L02 |
| **目标** | 掌握写规格的核心技能：**用户故事 + 可验收的验收标准**。这是整门课最重要的单项技能 |
| **动手** | ① `specify init focuslog` ② 写 constitution ③ `/speckit-specify` 描述 focuslog ④ 逐条审阅生成的 `spec.md` ⑤ 用 EARS 句式重写所有验收标准 |
| **EARS 句式** | `WHEN <触发> IF <条件> THE SYSTEM SHALL <响应>` —— 详细见 `reference/ears.md`。这是把「支持导出周报」翻译成机器不会误解的语句的工具 |
| **交付物** | `specs/001-.../spec.md`，含 3–5 个用户故事，每个 3 条以上 EARS 验收标准 |
| **验收** | ① spec 里**搜不到任何技术栈词汇**（无框架名、无数据库名）② 每条验收标准都存在一个能让它失败的测试用例 ③ 每个用户故事都能独立演示 |
| **踩坑** | ① 忍不住写「用 Typer 实现」——污染 what 层 ② 验收标准写成「用户体验良好」——不可验收 ③ 一个故事塞八个功能 |
| **术语** | user story、acceptance criteria、EARS、out of scope |

---

## L04 · Plan & Tasks：技术方案与原子拆解

| 项 | 内容 |
|----|------|
| **时长** | 120 min |
| **前置** | L03 |
| **目标** | 第一次体验「**规格与规划分层**」：技术决策只允许出现在 plan，且必须能追溯到 spec 的某一条要求 |
| **动手** | ① `/speckit-plan` 传入技术栈（Python 3.10 / Typer / uv / pytest / SQLite）② 审阅 `plan.md`：每个技术选择都标注它服务哪条用户故事 ③ `/speckit-tasks` 生成 `tasks.md` ④ **人工重排任务依赖顺序**，这一步不要偷懒交给 agent |
| **好任务的标准** | 可独立验证 / 依赖明确 / 单个任务改动范围可控 / 完成后能立即跑一次验收 |
| **交付物** | `plan.md` + `tasks.md` |
| **验收** | ① plan 中每项技术决策都能指出对应的 spec 条目，**没有凭空出现的依赖** ② tasks 中每个任务能映射回一条验收标准 ③ 存在至少一个任务被你有理由地拆得更细（证明你在做判断，不是照抄） |
| **踩坑** | ① 跳过 plan review 直奔 tasks → 后面返工加倍 ② 任务粒度全一样大（说明没真正拆）③ plan 里塞进 spec 没要求的功能（**范围蔓延的典型入口**） |
| **术语** | plan、task decomposition、dependency ordering、traceability（可追溯性） |

---

## L05 · Implement：受约束的实现与手工验收

| 项 | 内容 |
|----|------|
| **时长** | 120 min |
| **前置** | L04 |
| **目标** | 完成第一次「规格 → 代码」的忠实转换，并建立**验收纪律**：跑通 ≠ 完成 |
| **动手** | ① `/speckit-implement` ② 逐个任务对照 `spec.md` 手工验证 ③ **故意制造一次漂移**：手改一处代码让行为偏离规格，看自己能否察觉 ④ 修正并记录 |
| **交付物** | 可运行的 `focuslog`（`focuslog start --tag reading` / `stop` / `report --week` / `stats`）+ 一份验收记录 |
| **验收** | ① 每条 EARS 标准都有对应的验证记录，**不允许出现「应该没问题」** ② 每个用户故事都能端到端演示一遍 ③ 你能说出这次实现里哪一处偏离了规格、怎么发现的 |
| **踩坑** | ① 命令跑通了就宣布完成 ② agent 顺手实现了规格外的功能，你默许了（**这是漂移的起点**）③ 让 agent 自己写验收结论 |
| **术语** | convergence、verification、drift detection |

> ✅ **阶段二终点**：你已经掌握 spec-first 水平。但此刻规格**没有强制力**——代码可以悄悄偏离，没人报警。阶段三解决这件事。

---

# 阶段三 · 生产级规格锚定 ⭐

> 案例：**项目 B `taskflow`**（TypeScript + Fastify 协作 API）。
> 选它是因为：类型系统 + 契约测试 + CI 天然构成「三层强制力」，最能让规格漂移变成**红灯**而不是**事后发现**。
> 本阶段使用**完整路径**，引入全部质量门禁。
>
> ⚠️ **范围已刻意收紧**：**7 个端点、2 个角色**（owner / member），不是 12 个端点 3 个角色。
> 理由：本阶段的教学目标是**演示强制力**，不是建一个生产级协作平台。
> 端点越多、角色越细，噪音越大而「漂移变红灯」这件事并不会更清楚——
> **技术规模冒充教学价值，是我在 `projects/README.md` 里批评过的同一个错误，不能自己犯。**

## L06 · Clarify & Checklist：把歧义清零

| 项 | 内容 |
|----|------|
| **时长** | 120 min |
| **前置** | L05 完成 |
| **目标** | 学会用**流程化手段**消灭歧义。这是 SDD 相对 vibe coding 最直接的收益来源 |
| **动手** | ① `/speckit-specify` 描述 taskflow ② `/speckit-clarify` 聚焦易歧义区（状态流转规则、评论权限、并发冲突、鉴权边界）③ 回答 agent 的每个追问，**不许回答「你看着办」** ④ `/speckit-checklist` 生成需求质量清单，以**评审人身份**逐项打勾 |
| **关键区分** | 自定义 checklist 是**需求质量的评审工件**（打勾 = 我认为这条要求写清楚了），**不是**实现完成度。混淆这两者是最常见的误用 |
| **交付物** | 澄清后的 `spec.md` + 一份 checkbox 已评审的 checklist |
| **验收** | ① 澄清过程中记录下**至少 3 处「我原本以为很清楚其实不清楚」**的地方 ② checklist 中没有「靠猜」打勾的项 |
| **踩坑** | ① 跳过 clarify 直接 plan —— 在歧义上做规划，等于把钱花在错的图纸上 ② 把 checklist 当待办列表，实现完才打勾 |
| **术语** | ambiguity resolution、requirements quality、reviewer-owned artifact |

---

## L07 · 架构与契约规格：让 plan 有牙齿

| 项 | 内容 |
|----|------|
| **时长** | 120 min |
| **前置** | L06 |
| **目标** | 掌握「规格如何变成可执行约束」——这是 spec-anchored 的技术核心 |
| **动手** | ① `/speckit-plan`（Fastify + zod + vitest + Postgres）② 为**最关键的那一个**决策写 ADR：为什么这么选、代价是什么 ③ 把验收标准**翻译成契约测试**：schema 测试、状态机测试、权限矩阵测试 ④ 非功能需求**只写 3 条**：分页上限、并发冲突策略、错误码表 |
| **三种「牙齿」** | 类型约束（zod schema）/ 契约测试（vitest 用例）/ CI 门禁（漂移即失败） |
| **交付物** | `plan.md` + 1 份 ADR + 契约测试骨架 + **验收标准↔测试用例映射表** + 3 条非功能需求 |
| **验收** | ① 每条验收标准至少有一个自动化测试与之对应，**映射关系显式记录成一张表** ② 存在至少一条**故意写成会失败的测试**，用来证明测试真的能抓到偏离 |
| **踩坑** | ① 契约测试只测 happy path ② 非功能需求写成「性能要好」——要写成「列表接口 P95 < 200ms，单页返回 ≤ 100 条」③ 想一次把 ADR 写全——**只写给最贵的那一个决策**就够了 |
| **术语** | ADR、contract test、non-functional requirement、schema-as-constraint |

---

## L08 · Analyze & 分批 Implement：一致性与节奏

| 项 | 内容 |
|----|------|
| **时长** | 120 min |
| **前置** | L07 |
| **目标** | 学会在动手前发现跨文档矛盾，并掌握**分阶段实现**的节奏控制 |
| **动手** | ① `/speckit-analyze` 检查 spec ↔ plan ↔ tasks 的一致性 ② **故意留一处矛盾**（例如 spec 说任务只有两个状态、plan 的状态机画了四个），看 analyze 能否抓出来 ③ 修复后重跑 ④ `/speckit-implement` 按阶段实现，**每个阶段停下来人工验收**，不一次性跑完全部任务 ⑤ 开第二个 feature，演练 feature 切换 |
| **为什么必须分批** | 一次性跑完 = 错误在无人区累积。阶段边界是人类唯一有效的纠偏点 |
| **新增：多 feature 工作流** | 前面所有课都是单 feature（`001-*`）从零走到收敛，这不是真实工作。本节要练两件事：① 如何**开第二个 feature**而不扰乱第一个的上下文 ② 如何用 `.specify/feature.json`（或 `SPECIFY_FEATURE_DIRECTORY`）在两者间切换。命令靠**状态文件**而非 git 分支解析上下文——这件事在 L00 只是提了一句，现在你要真的踩到它 |
| **交付物** | analyze 零冲突报告 + 分阶段 PR + 每阶段验收记录 + 第二个 feature 的目录与切换记录 |
| **验收** | ① analyze 报告 clean ② 至少一个阶段因人工验收而**调整了任务顺序或拆解方式** ③ 你能演示在两个 feature 间切换上下文，且两边文档互不污染 |
| **踩坑** | ① analyze 报冲突却选择「先实现再说」 ② 一次性 implement 全部任务，出问题时无法定位是哪一层错了 ③ 开第二个 feature 时直接复用第一个的 spec/plan——宪法可复用，规格不可 |
| **术语** | cross-artifact analysis、incremental implementation、human checkpoint、多 feature 上下文 |

---

## L09 · Converge & 漂移治理 ⭐ 课程价值拐点

| 项 | 内容 |
|----|------|
| **时长** | 120 min |
| **前置** | L08 |
| **目标** | **达到 spec-anchored**：让规格真正成为活的、被强制执行的真相源 |
| **动手** | ① `/speckit-converge` 比对代码与 spec/plan/tasks，补上遗漏任务 ② 循环 `implement → converge` 直到报告 **Converged** ③ **上 CI 门禁**：任何让契约测试失败、或让 spec 与实现脱节的改动一律红灯 ④ **红蓝对抗**：让另一位 agent（或你自己）刻意制造行为漂移，验证门禁能抓住 ⑤ **走一遍完整的规格变更**：改一条已有的验收标准 → 看哪些工件必须跟着变 ⑥ 算一笔成本账 |
| **新增：规格变更流程** | 前面 9 节都是「从零走到收敛」，但真实工作里更常见的是——**功能上线两周后要改行为**。本节要固化一套可重复的流程：改 `spec.md` 里的验收标准 → 重跑 `/speckit-analyze` 看下游是否被破坏 → 更新契约测试（先让测试变红）→ 改实现 → `/speckit-converge`。关键是**先改规格再改代码**，而不是反过来事后补文档 |
| **新增：converge 不收敛怎么办** | 循环报 gap 是常态，不是异常。要建立**三层分流判断**：gap 是缺实现（→ implement）？还是规格没写清（→ 回 clarify/specify）？还是任务拆错了（→ 回 tasks）？判定错层级，会陷入无限循环。本节要求你至少遇到一次不收敛并正确分流 |
| **新增：成本量化方法** | 验收要求你「量化成本」，所以得给你方法。两个可操作指标：① **返工次数**——每 10 个任务里有几个因为「理解错了」而重做（对比项目 A 凭记忆估）② **规格变更波及面**——改一条验收标准平均要动几个文件。把这两个数字写进笔记，这就是你的成本账 |
| **交付物** | Converged 报告 + 生效的 CI 配置 + 一次成功的漂移抓捕记录 + 一份规格变更记录 + 一页成本账 |
| **验收** | ① 漂移演练中门禁确实变红（**没变红就是本节没及格**）② 你能量化说明：spec-anchored 相比项目 A 多花了多少成本、多拿回了什么 ③ 遇到过一次 converge 不收敛，并**正确判定**了它属于哪一层 |
| **踩坑** | ① converge 报 gap 就手改代码绕过，而不回填 tasks ② 门禁太严导致每次提交都要改 spec，团队会关掉它——门禁范围要克制 ③ 不收敛时反复重跑 implement，而不去想是不是**规格本身错了** ④ 改行为直接改代码，事后再补 spec（这就退回了 vibe coding） |
| **术语** | converge、drift governance、CI gate、spec change workflow、delta spec |

> ✅ **阶段三终点**：你拥有了一套生产可用的 SDD 流程。绝大多数团队宣称在做 SDD，实际停在阶段二。

---

# 阶段四 · 进阶与自主化

## L10 · Spec-as-Source：当人只改规格

| 项 | 内容 |
|----|------|
| **时长** | 120 min |
| **前置** | L09 |
| **目标** | 亲手体验最高严谨度等级，同时**建立对它的边界感**——这是本节的真正价值 |
| **案例** | **项目 C `rulesmith`**：用规格声明报表规则 DSL，生成解析器 + 校验器 + 用户文档 |
| **动手** | ① 写 DSL 的规格（语法、语义、错误情形）② 让 agent 写**生成器**而非成品代码 ③ 改一处规格 → 全量再生成 → 验证产物 ④ 记录：哪些地方坚持住了「不手改」，哪些地方必须破例 |
| **交付物** | `rulesmith` 生成器 + 一次「只改 spec 完成变更」的完整演示 |
| **验收** | ① 版本库中**不存在手工编辑的生成产物**（用 CI 校验生成物可复现）② 你能明确说出本项目**不适合**规格即源的部分及原因 |
| **踩坑** | ① 把 spec-as-source 当终极目标，套到不适合的领域（UI、算法调优、探索性需求）② 生成器本身没人维护，比手写代码更脆 |
| **术语** | code generation、idempotent regeneration、generated artifact |

---

## L11 · 存量改造：给已存在的仓库补规格

> ⚠️ **本节已从原计划拆分。** 原来 L11 同时要做「七种工具横评」和「存量仓库逆向补规格」，但后者本身就是 3–4 小时的活。
> **工具横评已移到 L12**（工具选型本来就是团队决策，和落地放在一起更合理），L11 变成纯 brownfield 专题。

| 项 | 内容 |
|----|------|
| **时长** | 120 min（实操通常 3–4 h） |
| **前置** | L10 |
| **目标** | 掌握最贴近真实工作的技能：**给已经存在的代码库补规格**。你不会天天做新项目，但你会天天碰存量代码 |
| **动手（brownfield）** | ① 挑一个**你自己手头的**存量小仓库（不要用课程三个项目）② 按 Spec Kit 的存量项目指引接入 ③ **逆向工程**：从现有代码提炼 spec，标注两类偏差——「实现里有但规格没写」和「规格写了但实现没有」 ④ 每一处偏差都必须归类为「改代码」或「改规格」 |
| **动手（第二个交付物）** | ⑤ 接入方式本身就是信号：如果你发现**没法给这个仓库写出干净的规格**，记录下原因——往往是这个模块职责不清，这才是真问题 |
| **交付物** | 存量仓库的偏差清单（每处已归类）+ 一页「这个仓库适不适合 SDD」的判定 |
| **验收** | ① 偏差清单中**没有任何一处停留在「待定」** ② 你能说出接入过程中最难写规格的是哪个模块，以及为什么 |
| **踩坑** | ① 逆向补规格时把 bug 当成「现状」写进规格，把错误固化（判定标准：这个行为是**有意设计**的吗？）② 想一次补齐所有历史代码的规格——**按变更热点渐进，不要全量** ③ 给一个自己都不敢改的模块补规格 |
| **术语** | brownfield adoption、reverse specification、spec drift audit、变更热点 |

---

## L12 · 团队化落地

| 项 | 内容 |
|----|------|
| **时长** | 120 min |
| **前置** | L11 |
| **目标** | ① 建立**工具选型判断力**（从 L11 移来）② 从「我会用」到「团队能用」，并完成课程收束 |
| **工具横评** | Spec Kit（开源、模型无关、参考实现）/ Kiro（agentic IDE、hooks 守则、AWS 亲和）/ Claude Code `cc-sdd`（终端优先）/ Cursor Plan Mode（IDE 内 diff 审阅）/ OpenSpec（Markdown + YAML 极简）/ BMAD（多角色方法论）/ Tessl（合规与审计轨迹） |
| **内容** | ① 多 agent 协作与交接（规格作为交接契约，而非口头同步）② Spec Kit 的 presets / extensions / workflows / bundles 如何定制你自己的流程 ③ 评审流程：谁审 spec、谁审 plan、评审看什么 ④ 度量：漂移率、返工率、规格覆盖率 ⑤ 团队落地检查清单 ⑥ 失败模式复盘与**退出策略**：什么情况下应该放弃 SDD |
| **交付物** | 工具选型对比表 + 团队落地检查清单 + 你为自己项目定制的 SDD 流程（前两份有现成模板：`templates/tool-selection-template.md`、`templates/team-rollout-checklist-template.md`） |
| **验收** | ① 对比表能给出「什么团队该选什么」的具体建议 ② 能回答：如果明天要在一个 5 人团队推行这套流程，第一周做什么、第一个月看什么指标、什么信号意味着该收手 |
| **术语** | preset、extension、workflow、bundle、spec coverage、drift rate |

---

# 阶段五 · 可选进阶（1 节）

> 本节与前三阶段换了一根轴：不问「规格有多强」，而问「**同一个约束能放在多早执行**」。
> 它依赖 JDK 17 与 Maven；没装工具链时 `run-all-gates.sh` 会把这一项计入**跳过项**并单独列出（不会谎称全绿）。

## L13 · 把约束前移到构建期（可选）

| 项 | 内容 |
|----|------|
| **时长** | 120 min（含实操通常 4 h） |
| **前置** | L09（已在项目 B 里体会过「测试 + CI」这一级强制力） |
| **案例** | 项目 D `billflow`（Java 17 · Maven 多模块 · JUnit 5 · ArchUnit · 自制注解处理器） |
| **目标** | 掌握「约束的时机」这层判断力：同一个约束放在测试、CI、编译期、构造期，各自的代价与收益 |
| **动手** | ① 读 `spec.md` 与 `ADR-003`（三个取整决策的取舍）② 读 `spec-guard/`（~200 行的注解处理器）③ `mvn verify` 跑通 ④ **依次注入四条违规，确认它们真的会红** |
| **三种机制** | 编译期（编号不存在 → 编译失败）· 字节码（架构规则 → 定位到方法）· 构造期（不变量 → 非法对象无法创建） |
| **交付物** | 可构建的 Maven 工程 + 六条元验证记录（A–F，含「构建全绿但校验零执行」与「增量编译空跑」两条） |
| **验收** | ① `mvn verify` 全绿（751 个测试）② `@Ac("9.9")` → 编译失败 ③ domain 依赖 `double`/时钟 → 架构测试失败 ④ 覆盖率 21/21 ⑤ **你写下这套约束不适合自己手头哪个项目、以及原因** |
| **踩坑** | ① 必须在**聚合根**上跑 `mvn verify`（进子模块跑会静默跳过注解处理，构建全绿但什么都没查）② Windows 上 `MAVEN_OPTS="-Dfile.encoding=UTF-8"` 不是可选项 ③ 注解处理器没有 `close()` 钩子，收尾逻辑靠 `processingOver` ④ `LocalDate.now()` 漏过「类依赖」规则，必须写「方法调用」规则 ⑤ Java 17 上 sealed + 穷尽 switch 还是预览特性，所以本案选了注解处理器路线 |
| **术语** | annotation processor、ArchUnit、构建期强制、构造器不变量 |

> ⚠️ **本节刻意标为可选。** 它不引入新的 SDD 概念，而是把已有的「强制力」推到更早的时机。
> 学完后最重要的产出不是代码，而是那条判断：**什么时候不该上这级约束**。

---

# 阶段六 · 可选进阶（1 节）

> 上一阶段把约束往**早**推，本阶段往**晚**推 —— 推到应用程序已经不在了也拦得住。
> 两者是同一根轴的两端，所以**建议连着上**（L13 → L14），对照感最强。
> 它只用 Node ≥ 22.5（内置 `node:sqlite`）与 `sqlite3` 命令行，**零第三方依赖**；
> 缺 node 时 `run-all-gates.sh` 会把这项计入**跳过项**并单独列出。

## L14 · 把约束下沉到数据层（可选）

| 项 | 内容 |
|----|------|
| **时长** | 120 min（含实操通常 4 h） |
| **前置** | L09（已在项目 B 里体会过「测试 + CI」这一级强制力）。**在 L13 之后读最顺** |
| **案例** | 项目 E `stockflow`（SQLite · Node 内置 `node:sqlite` · **零依赖**） |
| **目标** | 掌握「约束可以住在多晚」这层判断力：一条规格若要拦住**不经过你的代码的写入**，它必须住在数据层；同时认清这层强制的**代价与边界** |
| **动手** | ① 读 `spec.md` 与四份 ADR（ADR-002 是本案核心）② 读 `migrations/001_init.sql`（实现的主体）③ `node --test tests/*.spec.mjs` ④ `bash scripts/check-sql-enforcement.sh` ⑤ **用 `sqlite3` 命令行直写脏数据，看哪一条漏了、为什么** |
| **三种机制** | 结构（`STRICT`/`CHECK`/`UNIQUE`/`TRIGGER`，随库走）· 协议（`FOREIGN KEY`，**需要每一条连接兑现前提**）· 降级（强制不了 → 巡检查询，让违规**可见**） |
| **交付物** | 一份承担强制力的 schema + 保证者映射表 + 「搬不过去的东西」清单 + 五条元验证记录 |
| **验收** | ① 测试 44 全过、门禁 20 项全过 ② 覆盖率 26/26 ③ 命令行直写 → 被引擎拒绝，且能说出哪一条漏了 ④ **找出你项目里一条「在应用层检查得过早」的规则并搬过去** ⑤ **说出一条不该搬进数据层的规格并给出理由**（最能体现判断力） |
| **踩坑** | ① `PRAGMA foreign_keys` 是**每连接**的，且不同客户端默认值不同（实测 Node 开、Python 与 CLI 关），而且在事务内设置会被**静默忽略** ② 不 `STRICT` 的 SQLite 是动态类型，「数量是整数」没被任何东西保证 ③ 不能按分号切分 SQL 文件（触发器体里本来就有分号）④ `store` 每个函数自带事务 → 组合调用报 `cannot start a transaction within a transaction`，用 SAVEPOINT 可重入 ⑤ `node:sqlite` 返回的行对象**原型是 `null`**，`deepStrictEqual` 会报「内容一模一样」的失败 |
| **术语** | 约束下沉、强制力的前提、结构 vs 协议、巡检、保证者、可重入事务、反假绿 |

> ⚠️ **本节同样刻意标为可选，而且比 L13 更该谨慎采纳。**
> 它的收益全部来自「存在绕过路径」，代价则全部付在**演化**上 ——
> SQLite 改一条 `CHECK` 或换一个触发器条件，一律要「建新表 → 搬数据 → 换名」。
> **如果你的系统没有绕过路径，这套东西对你就是纯负债。**

---

# 阶段七 · 可选进阶（1 节）

> 第三次换轴。L13 把约束往**早**推，L14 往**晚**推 —— 但两者的强制力都指向**代码**。
> 本节问的是：当写代码的是 agent，**规格本身可执行吗？执行规格的模型可靠吗？**
> 约束第一次不落在代码上，而落在「考卷」和「考生」上。
> 工具链要求最低：Python 纯标准库即可，模型可选 —— 没有 API key 也能人肉判定。

## L15 · 把验收标准变成评测集（可选）

| 项 | 内容 |
|----|------|
| **时长** | 90 min（含实操通常 2 h） |
| **前置** | L07（映射表）与 L09（验收纪律）。不依赖 L13 / L14 |
| **案例** | 复用项目 A `focuslog` 的 23 条标准，不新建项目 |
| **目标** | 建立「测试 ≠ 评测」的判断力：测试证明代码符合规格（确定性断言）；评测验证规格本身可执行、且换一个执行者仍成立（有方差）。并知道评测**为什么不能进 CI** |
| **动手** | ① `python scripts/generate-evals.py examples/focuslog --out notes/L15-evals.jsonl`（23 张卡）② 按「边界优先、跨标准组合一张」挑 3–5 张 ③ 逐条判定（人肉或模型，同一张卡至少两遍）④ 记录翻转并逐条归类：改 spec / 改 code / 标准不可验收 ⑤ 改 spec 的部分走 change-request 流程回馈 |
| **三种用途** | 规格质量评测（逐条找茬）· 验收评测（可重放的验收记录）· 回归评测（spec 变更后看哪些判定翻转） |
| **交付物** | 评测集 + 判定记录 + 每处非 PASS 的归类 |
| **验收** | ① 卡数与覆盖率门禁实测值一致 ② 至少 3 张卡有真实行为证据 ③ 每处 FAIL / 无法判定都归了类 ④ 说出评测不能进 CI 的两个理由 |
| **踩坑** | ① 把评测 PASS 当测试绿 ② 判定卡塞整份 spec ③ 丢掉「无法判定」这个结果 ④ 用「模型说 PASS」冒充证据 ⑤ spec 改了评测集不重生成 —— 与映射表脱节是同一种病 |
| **术语** | evaluation、eval set、judge、verifier card、flip |

> ⚠️ **本节同样刻意标为可选。** 它的适用面最窄：行为能全靠断言覆盖、或规格少而稳定的项目，
> 做评测是重复劳动。它最值得用的场景恰好是本课程的主场景 ——
> **规格交给模型执行，行为由人来验收**。

---

# 阶段八 · 可选进阶（1 节）

> 第四次换轴，也是约束光谱的最后一段。L13 管**早**、L14 管**晚**、L15 管**考卷** ——
> 但它们全部只保证「交付的瞬间是对的」。本节问：**上线三个月后，谁还在替规格站岗？**
> 工具链要求最低：全程可纸面完成，不需要任何新门禁 —— 这本身就是一个教学决策。

## L16 · 上线之后：让规格在生产里活着（可选）

| 项 | 内容 |
|----|------|
| **时长** | 60 min（含实操通常 2 h） |
| **前置** | L09。在 L14 之后读最顺（巡检查询是本节的雏形） |
| **案例** | 复用项目 E 的 `patrol.sql` 与项目 B 的错误码投影，不新建项目 |
| **目标** | 认识规格在生产里的三种静默失效（行为偏离 / 真相源分叉 / 非目标长出实现），并建立「哪些标准值得上探针」的三条判据 —— **判据是产出，基础设施不是** |
| **三种工具** | 生产探针（标准 → 遥测查询）· 契约交叉验证（消费方出题）· flag 对账（每个默认关的行为指回一条标准） |
| **动手** | ① 按判据从 focuslog 23 条标准里筛探针（大部分会被筛掉——这也是发现）② 给 taskflow 状态机做两消费方纸面推演，求断言交集 ③ 对自己手头的真实项目做一次 flag 对账 |
| **交付物** | 探针清单（每条含「半夜响了谁起床」的应答）+ 交叉验证交集 + 对账记录 |
| **验收** | ① 探针全部通过判据筛选并指回标准编号 ② 交集清单没有「指不回标准却算公共契约」的行 ③ 对账没有一行留在待定 ④ 说出本节为什么没有可运行的新门禁 |
| **踩坑** | ① 探针绑日志文案（一改就哑）② 把探针当门禁（响了三次必被静音）③ flag 永不归档 ④ 消费方契约写成对方义务 ⑤ 只有一个消费方就宣称交叉验证 |
| **术语** | probe、consumer-driven contract、flag debt、on-call budget |

> ⚠️ **本节刻意没有可运行的门禁**——探针有方差（与 L15 评测同理）、告警有噪声成本。
> 它的全流程可纸面完成，产出的是**判据**：等真有生产系统时，判据现在就该有，基础设施到时再建。

---

## 附：时间预算（修订版）

> ⚠️ **旧版本只写了「23 小时」，那是错的。** 它只算了上课和核心练习的时间，**没算真正把项目建出来**。
> 用 23 小时做预期，你会在 L05 就以为自己是废物。下面是修订后的两层预算。

| 阶段 | 节数 | 课时 + 核心练习 | 加上项目实操 |
|------|------|---------------|------------|
| 一 · 认知与地基 | 3 | 3.5 h | 4 h |
| 二 · 最短闭环 | 3 | 6 h | 12 h |
| 三 · 规格锚定 | 4 | 8 h | 17 h |
| 四 · 进阶自主化 | 3 | 6.5 h | 17 h |
| 五 · 可选进阶（L13） | 1 | 2 h | 4 h |
| 六 · 可选进阶（L14） | 1 | 2 h | 4 h |
| 七 · 可选进阶（L15） | 1 | 1.5 h | 2 h |
| 八 · 可选进阶（L16） | 1 | 1 h | 2 h |
| **合计** | **17**（含 4 节可选） | **约 31 h** | **约 62 h** |

**怎么用这两个数字**

- **31 小时**是你「跟着教程走一遍」的时间——每节 1–2 小时，每天一节约三周半，每周三节约六周。
- **62 小时**是「五个项目都能端到端演示」的时间。差出来的 31 小时全在**写代码、跑测试、跟 CI 打架**上，而这部分恰恰是课程真正的价值所在。
- 时间不够时的**最佳减法**：跳过项目 C（L10，省 6h）、**L13 / L14（各省 4h）、L15（省 2h）与 L16（省 2h）**，把项目 B 的 CI 门禁先做成「只警告不阻断」。**不要跳过 L09**——那是整门课的目标。
- L13 与 L14 **不该被当成「更高级所以更重要」**。它们换的是另一根轴（约束的时机与位置），与 A/B/C 的三级成熟度并不构成递进；忙的时候它们就是第一个该砍的。L15 更靠后：它约束的不是代码，是执行规格的 agent。
- 但 L13 / L14 **是最值得对照读的两个**：只读一个，你容易得出「越早越好」或「全都下沉」这种单向结论 ——
  而这两条结论在生产里都是错的。

**关键里程碑**：L05 = spec-first 达成；**L09 = spec-anchored 达成（真正的目标）**；L10 = spec-as-source 体验；L12 = 可落地。

---

## 附：每节课产出的规格严谨度进展

```
L00–L02  ░░░░░░░░░░  无规格（只有 hello-sdd 空跑）
L03–L05  ███░░░░░░░  Spec-First        → 项目 A，规格是种子，无强制力
L06–L09  ███████░░░  Spec-Anchored ⭐  → 项目 B，CI 门禁，漂移即红灯
L10      ██████████  Spec-as-Source    → 项目 C，人只改规格
L11–L12  ████████░░  按场景选择        → 存量改造 + 团队落地，懂得何时不用它
L13      ░░░░░░████  约束前移          → 项目 D，写错的人拿不到可运行产物
L14      ████░░░░░░  约束下沉          → 项目 E，绕过应用也拦得住
L15      ░░░░░███░░  评测              → 规格变成 agent 的考卷
L16      ░░░░░░███░  运行期            → 上线之后，谁在替规格站岗
```

> L13 / L14 的进度条不接在「严谨度」上 —— 因为它们换的是**轴**，不是等级。
> 把这两行读成「比 L10 更高级」是最常见的误读，而它会导致在两个错误的时机上约束。
> L15 同样不接 —— 它约束的不是代码，是执行规格的 agent。
> L16 也不接 —— 它管的是「交付之后」的时间轴，四节可选课各守一根轴：早 / 晚 / 考卷 / 站岗。

---

## 附：修订记录

**v2（本次审阅后修订）**——修掉四个结构性缺陷：

| # | 缺陷 | 修法 |
|---|------|------|
| 1 | **时间预算低估约一半**：只算课时不算建项目 | 拆成「课时 / 含实操」两层（v2 时为 **28h / 58h**，含两节可选；v4 起 30h / 60h，见下），并给出时间不够时的减法优先级 |
| 2 | **L11 塞了两节课的量**（工具横评 + brownfield） | 工具横评移到 L12（工具选型本来就是团队决策）；L11 变成纯 brownfield 专题 |
| 3 | **项目 B 范围超出教学需要**（12 端点 / 3 角色） | 收到 7 端点 / 2 角色。端点多不会让「漂移变红灯」更清楚，只会加噪音 |
| 4 | **两个真实缺口** | 补上**规格变更流程**（L09：改行为先改规格的完整链路）、**converge 不收敛的分流判断**（L09）、**多 feature 工作流**（L08）、**成本量化方法**（L09） |

缺陷 3 值得一提：我在 `projects/README.md` 里批评过「技术规模冒充教学价值」，然后在项目 B 的原始设计里犯了一模一样的错。**规划也要被审阅，不能只审代码。**

**v3（后续追加）**——加了两个可选进阶阶段，它们不是「更高级的 A/B/C」：

| # | 追加 | 它换来的是什么 | 为什么单独成阶段 |
|---|------|-------------|----------------|
| 1 | **L13 · 约束前移到构建期**（项目 D · Java） | 写错编号的人**拿不到可运行产物** | 它换的是「时机」这根轴，与 A/B/C 的「严谨度」不构成递进 |
| 2 | **L14 · 约束下沉到数据层**（项目 E · SQLite） | **不经过应用的写入**也拦得住 | 它换的是「位置」这根轴，且收益全部来自「存在绕过路径」这个前提 |

两节必须**成对读**：只读 L13 容易得出「约束越早越好」，只读 L14 容易得出「全都下沉到数据层」，
而这两条结论在生产里都是错的 —— 正确的结论是**按「会不会被绕过」与「规则稳不稳定」两个条件加权判断**。

**v4（本次追加）**——补上生态缺口的最后一环：

| # | 追加 | 它换来的是什么 | 为什么单独成阶段 |
|---|------|-------------|----------------|
| 1 | **L15 · 把验收标准变成评测集**（复用项目 A `focuslog`） | 规格从「给人看、给测试断言」多出一个身份——**给 agent 的考卷**；验收从「看过」变成**可重放** | 第三根轴：约束作用在**执行者**身上，与 L13 的「时机」、L14 的「位置」都不同 |
| 2 | 反模式 **AP-29**（范式冒充套件）+ 脚本 `generate-evals.py` | 把 taskflow 契约测试可执行化时抓到的真 bug（「14 格全覆盖」元验证漏算 POST/GET 两格）沉淀成可复用的教训 | 脚本与覆盖率门禁**同源判据**：两个工具对「什么是一条标准」只有一种理解，卡数可直接互检 |

> L15 的取舍记录在讲义 §6：行为能全靠断言覆盖的项目不需要它。
> 它被放在最后、且标可选，是因为它是全课程**适用面最窄**的一节 ——
> 但在「规格交给模型执行、行为由人来验收」这个主场景里，它是唯一能回答
> 「规格换一个执行者还成立吗」的工具。

**v5（本次追加）**——补齐约束光谱的最后一段：

| # | 追加 | 它换来的是什么 | 为什么单独成阶段 |
|---|------|-------------|----------------|
| 1 | **L16 · 上线之后：让规格在生产里活着**（复用项目 E 的巡检与项目 B 的错误码投影） | 前面所有课保证「交付的瞬间是对的」，L16 回答「交付之后谁站岗」：生产探针 / 契约交叉验证 / flag 对账 | 第四根轴：前三节管「写出来」，这一节管「跑起来之后」 |
| 2 | 机器可读规格层：`check-error-codes.py` 门禁 + L07 §7 投影小节 | spec 的表格（人读）与实现里的字符串（机器写）之间的漂移有了机械拦截；`--json` 投影成为监控与探针的数据源 | 它是 L07「错误码表」的收尾，也是 L16 探针的数据源——一处沉淀，两节受益 |
| 3 | 规格治理：`spec-governance-template.md` 台账 + L12 §2 台账小节 | spec 超过三份后回答「归谁、还活着吗、谁在消费」；owner 是人名不是团队名，deprecated 不删除 | 治理不是新能力，是既有工件（spec / change-request / 复审）的**资产化**，归 L12 的团队层 |

> v3 加的 L13/L14 是「约束的时机与位置」，v4 的 L15 是「约束的执行者」，
> v5 的 L16 补上「约束的时间轴」。至此四根可选的轴全部成对/成组出现，
> 每一节都显式写了「不该用的场景」—— 这是本课程对每根轴的一贯要求：
> **给你工具的同时给你放弃它的判据。**
