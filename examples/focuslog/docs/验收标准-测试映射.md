# 验收标准 ↔ 测试用例映射 · focuslog

> **本文件是 L07 的完整参考产出（在项目 A 上的最小版本）。**
> 在项目 A 里它的用途是「证明每条验收标准都可测」；在项目 B 里它会上 CI 作为阻断门禁。

**对应规格**：`specs/001-focuslog-mvp/spec.md`
**标准总条数**：23（脚本实测值，见文末自检输出）

> 📌 **本表曾经是 22 条。** 第 23 条（`3.7`）是一轮**外部审阅**（见 `notes/L06-LLM审阅记录.md`）
> 挖出来的跨周边界缺口。
> **新标准出现后没补映射行，门禁立刻变红** —— 这正是它该干的事：
> **新增一条没人测的标准，比不写它更危险**（它看起来已被覆盖）。

---

| # | 验收标准（简写） | 测试文件 :: 用例名 | 类型 |
|---|----------------|------------------|------|
| 1.1 | `start` 无活跃会话时创建并退码 0 | `test_cli_start.py::test_start_creates_session` | 行为 |
| 1.2 | `start` 有活跃会话时拒绝，退码 2 | `test_cli_start.py::test_start_rejected_when_active` | 边界 |
| 1.3 | `start` 无 `--tag` 退码 1，`TAG_REQUIRED` | `test_cli_start.py::test_start_requires_tag` | 参数 |
| 1.4 | 标签为空/纯空白/超 32 字符 → `TAG_INVALID` | `test_tags.py::test_tag_invalid_forms` | 参数 |
| 1.5 | 标签含非法字符 → `TAG_INVALID` | `test_tags.py::test_tag_illegal_chars` | 参数 |
| 1.6 | 重复 `start` 幂等，开始时间不变 | `test_cli_start.py::test_start_idempotent` | 幂等 |
| 2.1 | `stop` 记录结束时间并输出时长 | `test_cli_stop.py::test_stop_records_and_reports` | 行为 |
| 2.2 | `stop` 无活跃会话时退码 2 | `test_cli_stop.py::test_stop_without_active` | 边界 |
| 2.3 | 重复 `stop` 第二次拒绝，首次 `ended_at` 不变 | `test_cli_stop.py::test_stop_idempotent` | 幂等 |
| 2.4 | 时长不足 1 分钟记 1 分钟并提示 | `test_durations.py::test_sub_minute_rounded_up` | 边界 |
| 2.5 | 跨天会话全部计入开始日并提示 | `test_durations.py::test_cross_day_attributed_to_start` | 边界 |
| 3.1 | 周报覆盖 ISO 周（周一至周日） | `test_weeks.py::test_iso_week_bounds` | 边界 |
| 3.2 | 周报含周起止、每日时长条数、标签分布 | `test_report.py::test_report_contains_all_sections` | 行为 |
| 3.3 | 空周报退码 **0** 并输出提示 | `test_cli_report.py::test_empty_week_exit_zero` | 边界 |
| 3.4 | `--json` 字段与 3.2 一一对应 | `test_json_out.py::test_report_json_fields` | 契约 |
| 3.5 | 标签分布按 `(-duration, tag)` 排序 | `test_sorting.py::test_duration_sort_key` | 边界 |
| 3.6 | 周报排除活跃会话，末尾提示 | `test_report.py::test_active_excluded_and_noted` | 边界 |
| 3.7 | 邻近周会话不计入本周，输出条数提示 | `test_weeks.py::test_adjacent_week_note` | 边界 |
| 4.1 | `stats` 按标签时长降序 | `test_cli_stats.py::test_stats_ordered` | 行为 |
| 4.2 | 时长相同时按标签字典序升序 | `test_sorting.py::test_tie_break_by_tag` | 边界 |
| 4.3 | 无记录时输出"暂无记录"退码 0 | `test_cli_stats.py::test_stats_empty` | 边界 |
| 4.4 | 输出格式为「标签 · 时长 · 条数」 | `test_cli_stats.py::test_stats_format` | 契约 |
| 4.5 | `stats --json` 字段与 4.4 一致 | `test_json_out.py::test_stats_json_fields` | 契约 |

---

## 元验证（测试有效性）

| # | 用例 | 说明 |
|---|------|------|
| — | `test_meta.py::test_sanity_check_fails` | 断言一个 spec 规定为**非法**的行为为合法（`todo` 类任务的越权写入）。**此测试必须失败。** 若它通过，说明测试环境或断言机制失效 |

**如何验证它确实有效**：

```bash
$ uv run pytest tests/test_meta.py -v
FAILED tests/test_meta.py::test_sanity_check_fails - AssertionError  ← 期望看到 FAILED
```

执行完确认后将其标记为 `@pytest.mark.skip`，并在本表保留记录。

---

## 覆盖率自检（自动化）

```bash
$ bash scripts/check-spec-coverage.sh examples/focuslog
spec 标准条数: 23
映射表行数:    23
✅ 覆盖完整
```

**两个数字必须相等。** 脚本见 `scripts/check-spec-coverage.sh`。

> 在项目 B（taskflow）里，这条命令会被搬进 CI 作为**阻断门禁**——
> 那是 spec-first 与 spec-anchored 的分界线。

---

## 按类型分布

| 类型 | 条数 | 说明 |
|------|------|------|
| 行为 | 4 | 主路径（1.1 / 2.1 / 3.2 / 4.1） |
| 边界 | 11 | **最多的一类** —— 因为 spec 阶段的 clarify 把边界都显式化了（含审阅补入的 3.7） |
| 参数 | 3 | 输入校验（1.3 / 1.4 / 1.5） |
| 幂等 | 2 | 最容易在实现里被漏掉的一类（1.6 / 2.3） |
| 契约 | 3 | 输出格式与字段（3.4 / 4.4 / 4.5） |
| **合计** | **23** | 不含元验证——元验证是额外的一条，不占这 23 格 |

**注意「边界」是最大的一类（11 条，占 48%）。** 这是 clarify 阶段主动穷举异常分支的直接结果——
如果只看主路径，这 10 条全都不会存在，而它们正是上线后最常出事的地方。

> ⚠️ **本表的数字由脚本实测得出，不是手写的。** 我最初在正文里写成了 21，
> 被 `check-spec-coverage.sh` 抓出来了——**这正是把检查机械化的价值：人的手数会错，脚本不会。**
